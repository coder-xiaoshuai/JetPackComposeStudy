<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jetpackcomposestudy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetPackComposeStudy">
        <activity android:name=".RecyclerViewActivity"></activity>
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetPackComposeStudy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".list.ListTestActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".login.LoginActivity" />

        <activity android:name=".ImagesActivity" />

        <activity android:name=".demo.CheckBoxActivity" />

        <activity android:name=".demo.DialogDemoActivity" />

        <activity android:name=".demo.LifeCycleDemoActivity" />

        <activity android:name=".demo.AnimationDemoActivity" />

        <activity android:name=".demo.SizeDemoActivity" />

        <activity android:name=".demo.ImageListDemoActivity" />
    </application>

</manifest>